export type serverRes = {
    description: string,
    id: string;
    name: string,
    thumbnail: { path: string, extension: string};
}
export type heroes = {
    description: string,
    id: string;
    heroName: string,
    thumbnail: string;
}
