export interface serverRes {
    description: string,
    id: string;
    name: string,
    thumbnail: { path: string, extension: string};
}
export interface heroes {
    description: string,
    id: string;
    name: string,
    thumbnail: string;
}
